# POLIS development dependencies — Implementation Plan task 1.3
-r requirements.txt

# ---- Test ----
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
respx==0.21.1                 # httpx mocking for ingestion adapter tests

# ---- Lint / format ----
ruff==0.6.9
black==24.10.0

# ---- Security (CI gates, SEC-18 / SEC-24) ----
pip-audit==2.7.3
pre-commit==4.0.1
